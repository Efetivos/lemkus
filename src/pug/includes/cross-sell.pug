|   {% assign $id =  product.id %}





//- =========================  CARD CROSS SELL  ========================= -//
//- =========================  CARD CROSS SELL  ========================= -//
|   {% for collection in product.collections limit:1 %}
section.dropping-card.dropping-card-regular
    |   {% for product in collections[collection.title].products limit:1 %}
    .dropping-card__hold

        //- BG
        .dropping-card__bg(style="background:#eaeaea")

        .dropping-card__infos
            //- CARD
            .dropping-card__logo 
                //img(src="{{ section.settings.img_logo_brand | img_url: 'master' }}", alt='{{ section.settings.img_logo_brand.alt | escape }}').img-load

            //- TITLE
            a(href="{{ product.url | within: collection }}").dropping-card__title.dharma {{ product.title }}

            //- PRICE
            .dropping-card__price.recosemi {{ product.price | money }}

            //- INFOS
            .dropping-card__infos__img.no-desk
                a(href="{{ product.url | within: collection }}") 
                    img(src="{{  product.featured_image.src  | img_url: 'master' }}" alt="{{ product.src | escape }}").img-load

            a(href="{{ product.url | within: collection }}").dropping-card__notify.btn-hover.cream-v
                    span.static VIEW PRODUCT
                    span.hover(data-hover="VIEW PRODUCT VIEW PRODUCT ") VIEW PRODUCT VIEW PRODUCT 


        .dropping-card__img.no-mobile
            a(href="{{ product.url | within: collection }}") 
                img(src="{{  product.featured_image.src  | img_url: 'master' }}" alt="{{ product.src | escape }}").img-load

    |   {% endfor %}
|   {% endfor %}











//- =========================  COLLECTION CROSS SELL  ========================= -//
//- =========================  COLLECTION CROSS SELL  ========================= -//

nav.cross-sell
    |   {% for collection in product.collections limit:1 %}
    .cross-sell__hold.colle-cards
        |   {% for product in collections[collection.title].products limit:6 %}
        |   {% if product.id != $id %}
        |   {% render 'product-card' prod: product %}

        |   {% endif %}
        |   {% endfor %}

    |   {% endfor %}

    .cross-sell__drag.e-hold
        .cols_index__drag.no-mobile
            .drag__cntrl
                .drag__cntrl__lines
                    .drag-line.line-static
                    .drag-line.line-dyna
                button(href="#").drag.recosemi DRAG