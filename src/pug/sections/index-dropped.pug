.   
    {% schema %}
        {
            "name": "INDEX | Just Dropped",
            "class": "dropped-index",
            "settings": [
                {
                    "id": "dropped_index",
                    "type": "collection",
                    "label": "Collection"
                },
                {
                    "id": "text_dropped",
                    "type": "text",
                    "label": "Text Dropped",
                    "default": "JUST DROPPED"
                }
            ]
        }
    {% endschema %}

| {% assign dropped_col = collections[section.settings.dropped_index] %}
section.collect-index

    h1.collect-index__title.dharma {{ section.settings.text_dropped }}
    .collect-index__hold.colle-cards
        | {% for item in dropped_col.products limit: 3 %}
        
        //- calling prod
        |   {% render 'product-card' prod: item %}

        | {% endfor %}


    .cols_index__drag.e-hold.no-mobile
        .drag__cntrl
            .drag__cntrl__lines
                .drag-line.line-static
                .drag-line.line-dyna
            button(href="#").drag.recosemi DRAG

    


